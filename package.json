{
	"name": "girphan_backend",
	"version": "1.0.0",
	"main": "index.ts",
	"license": "MIT",
	"dependencies": {
		"express": "^4.17.1"
	},
	"scripts": {
		"start": "node index.ts",
		"heroku-postbuild": "cd client && yarn && yarn run build"
	},
	"devDependencies": {
		"@types/node": "^12.0.8"
	}
}
